# calculator

Tiny calculator module with basic arithmetic operations.

Usage
-----

From the project root you can run the CLI:

```bash
python -m calculator.cli add 1 2
python -m calculator.cli div 10 2
```

Or import the functions in Python:

```python
from calculator import add, div
print(add(1, 2))
```
